# Task Drafts

Task drafts capture scoped work before it is promoted into
`docs/planning/tasks.yml`.

Generate a draft with:

```bash
python3 scripts/generate.py task-draft T-999 --title "Example task" --slug example
```
